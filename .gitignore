node_modules
.next
out
.env*.local
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# explicit: never commit the supabase service-key env (leaked once in d72a789)
supabase/.env.local

# Claude Code local machine state (personal permission allowlist)
.claude/settings.local.json

# Merchant CRM export — contains third-party business contact details and
# internal sales-pipeline notes. This repo is PUBLIC; keep this out of it.
supabase/merchant_crm.sql
